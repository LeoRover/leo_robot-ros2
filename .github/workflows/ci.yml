name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - ros2
  pull_request:
    branches:
      - ros2

jobs:
  industrial_ci:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        ROS_DISTRO: [jazzy, kilted, rolling]
        ROS_REPO: [testing, main]
    env:
      ROS_DISTRO: ${{ matrix.ROS_DISTRO }}
      ROS_REPO: ${{ matrix.ROS_REPO }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Source tests
        uses: ros-industrial/industrial_ci@125164b9f1883cdf1858897a7146d1bebf2be5c6 # master
        env:
          UPSTREAM_WORKSPACE: "github:LeoRover/leo_common-ros2#ros2"
          PYLINT_CHECK: true
          PYLINT_ARGS: "--rcfile=src/leo_robot-ros2/.pylintrc"
